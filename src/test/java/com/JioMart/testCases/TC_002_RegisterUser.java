package com.JioMart.testCases;

import org.testng.annotations.Test;

import com.JioMart.base.base;

public class TC_002_RegisterUser extends base {

	/*
	 * only if the number entered is a Register Number then only it can go in Sign
	 * Up and verify the number entered and the number displayed on the Sign up
	 * page. Add a if condition if the number entered is invalid / login number then
	 * the verify case should not be tested, it should be in If condition for when
	 * the number entered is a Register number.
	 */

	/*
	 * have a separate TC for Verify number entered and number displayed on Sign Up
	 * UI.
	 */

	@Test
	public void verify() {
		
	}

}
